export const CUSTOMER_CONFIG = {
    searchFields: ['name', 'email', 'phone'],
    tableColumns: ['name', 'email', 'phone', 'createdAt'], // for table rendering
    sortableFields: ['name', 'email', 'createdAt'], // for future sorting
};